package com.customer.fivecanale.util;

import android.view.View;

/** Interface for back button click */
public interface BackButtonClickListener {
  /**
   * Listens the click event for back button
   *
   * @param view view which is clicked
   */
  void backButtonClickListener(View view);

}